% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_nmf_k_sweep_sc_gpu}
\alias{rs_nmf_k_sweep_sc_gpu}
\title{Sweep k on the GPU and report consensus stability against error}
\usage{
rs_nmf_k_sweep_sc_gpu(
  f_path_gene,
  gene_indices,
  cell_indices,
  k_range,
  preprocessing,
  use_second_layer,
  nmf_hals_params,
  nmf_consensus_params,
  n_runs,
  seed,
  verbose
)
}
\arguments{
\item{f_path_gene}{Path to the \code{counts_genes.bin} file.}

\item{gene_indices}{Integer vector. 0-indexed(!) positions of the genes
to include.}

\item{cell_indices}{Integer vector. 0-indexed(!) positions of cells to
include in the analysis.}

\item{k_range}{Integer vector. Ranks to evaluate, every entry at least 2 and
at most 128, the GPU solver's rank cap.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on the normalised
counts; if \code{FALSE}, on the raw counts.}

\item{nmf_hals_params}{Named list. Contains the NMF parameters.}

\item{nmf_consensus_params}{Named list. Contains the consensus parameters.}

\item{n_runs}{Integer. Number of restarts per k. Must be at least 2.}

\item{seed}{Integer. Base random seed.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list of equal-length vectors, one element per swept k
\itemize{
\item k - The rank.
\item stability - Mean silhouette of the consensus clusters. \code{NaN} where
the consensus step failed.
\item best_error - Lowest restart error, relative to the squared
Frobenius norm of the input.
\item median_error - Median restart error, same scale.
\item consensus_failed - Did the density filter leave fewer than \code{k}
components.
\item n_dropped - Number of pooled components removed.
\item n_empty_clusters - Number of clusters left with no members.
\item n_converged - Restarts that met the HALS tolerance.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
GPU counterpart of \code{\link[bixverse:rs_nmf_k_sweep_sc]{bixverse::rs_nmf_k_sweep_sc()}}. Returns diagnostics
only, no factors, so a wide \code{k_range} stays cheap in memory. This is the
shape the GPU path is really for: the counts are uploaded once and serve
every one of the \code{length(k_range) * n_runs} solves. Pick the k where
stability is high and the error curve has not yet flattened, then call
\code{\link[=rs_nmf_consensus_sc_gpu]{rs_nmf_consensus_sc_gpu()}} there.
}
\references{
Kotliar et al., eLife, 2019
}
\keyword{internal}
