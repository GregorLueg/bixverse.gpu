% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_fast_cluster_grid_gpu}
\alias{rs_fast_cluster_grid_gpu}
\title{GPU: fast Louvain clustering on the data (with multiple seeds)}
\usage{
rs_fast_cluster_grid_gpu(
  embd,
  resolutions,
  n_centroids,
  fc_params,
  snn,
  return_kmeans,
  no_seeds,
  seed,
  verbose
)
}
\arguments{
\item{embd}{Numeric matrix. The original embedding.}

\item{resolutions}{Numeric vector. The Louvain resolutions to iterate
through.}

\item{n_centroids}{Optional integer. The number of clusters to find. If
not provided, defaults to \code{sqrt(nrow(embd))}.}

\item{fc_params}{Named list. See \code{\link[=params_sc_fast_cluster_gpu]{params_sc_fast_cluster_gpu()}}.}

\item{snn}{Boolean. Shall the kNN graph be additionally transformed into
an sNN graph.}

\item{return_kmeans}{Boolean. Shall the k-means centroids and assignments
be returned alongside the grid results.}

\item{no_seeds}{Integer. Number of additional seeds to use. Should be >= 2.}

\item{seed}{Integer. For reproducibility.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements:
\itemize{
\item membership - A list with \code{memberships} (the labels from the seed with
the best conductance, per resolution) and \code{stats} (the metrics per
resolution).
\item k_means_cluster - Optional integer vector of k-means assignments.
\item centroids - Optional numeric matrix of k-means centroids.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
GPU equivalent of \code{bixverse::rs_fast_cluster_sc_grid}. Builds the k-means to
kNN/sNN graph once, then runs Louvain with several seeds (derived from the
original one) for every resolution. Returns additional metrics around
cluster stability and community conductance. Only the k-means runs on the
GPU.
}
\keyword{internal}
