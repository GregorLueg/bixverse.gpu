% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_gpu.R
\name{harmony_v2_gpu_sc}
\alias{harmony_v2_gpu_sc}
\title{Run Harmony v2 (GPU)}
\usage{
harmony_v2_gpu_sc(
  object,
  batch_column,
  modality = c("rna", "adt"),
  harmony_params = params_sc_harmony_v2_gpu(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} class.}

\item{batch_column}{String. Column name in the object containing the batch
labels.}

\item{modality}{String. One of \code{c("rna", "adt")}. You can only use \code{"adt"}
on \code{SingleCellsMultiModal} class.}

\item{harmony_params}{List. Output of \code{\link[=params_sc_harmony_v2_gpu]{params_sc_harmony_v2_gpu()}}.}

\item{seed}{Integer. For reproducibility.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
The object with a \code{"harmony_gpu"} embedding added. If no PCA
embeddings are found, returns the object unchanged with a warning.
}
\description{
A GPU-accelerated version of Harmony v2 by Patikas et al., 2026,
implemented in Rust. Performs batch correction on PCA embeddings and stores
the result as a \code{"harmony_gpu"} embedding in the object. Only a single
batch covariate is supported on the GPU path.
}
