% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_seacells_gpu}
\alias{rs_seacells_gpu}
\title{GPU: SEACells meta cell generation}
\usage{
rs_seacells_gpu(
  f_path,
  embd,
  cells_to_keep,
  cells_to_use,
  knn_data,
  seacells_params,
  target_size,
  seed,
  verbose
)
}
\arguments{
\item{f_path}{String. Path to the \code{counts_cells.bin} file.}

\item{embd}{Numeric matrix. Cells x components embedding, one row per
QC-passing cell.}

\item{cells_to_keep}{Optional integer vector. 0-indexed original row indices
the embedding was built from, in embedding row order.}

\item{cells_to_use}{Optional integer vector. 0-indexed original row indices
to narrow the run to. Forces a kNN rebuild on that subset.}

\item{knn_data}{Optional list. Precomputed kNN graph with \code{indices}, \code{dist},
\code{dist_metric} and \code{k}. Ignored when \code{cells_to_use} is set.}

\item{seacells_params}{Named list. See \code{\link[bixverse:params_sc_seacells]{bixverse::params_sc_seacells()}}.}

\item{target_size}{Double. Library target size the meta cells are
normalised to.}

\item{seed}{Integer. Random seed.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal; \code{2L} - detailed.}
}
\value{
A list with the cell assignments, the aggregated meta cell counts
in compressed sparse form, the RSS history and the archetype cell indices.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
GPU equivalent of \code{bixverse::rs_get_seacells}. Both Frank-Wolfe solves, the
B-gradient argmin and the per-cell A columns, are dispatched to the WGPU
backend. The kNN graph, the kernel matrix, the RSS evaluation and the
aggregation into pseudo-bulk counts all stay on the CPU.
}
\references{
Persad, et al., Nat. Biotechnol., 2023.
}
\keyword{internal}
