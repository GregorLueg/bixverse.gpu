% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenic_gpu.R
\name{scenic_grn_sc_gpu}
\alias{scenic_grn_sc_gpu}
\title{GPU-accelerated SCENIC GRN inference}
\usage{
scenic_grn_sc_gpu(
  object,
  tf_ids,
  scenic_params = bixverse::params_scenic(),
  wave_byte_budget = 4 * 1024^3,
  genes_to_take = NULL,
  cells_to_take = NULL,
  streaming = NULL,
  random_seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{MetaCells} class from \code{bixverse}.}

\item{tf_ids}{Character vector. Gene identifiers of the transcription
factors to use as predictors.}

\item{scenic_params}{Named list. See \code{\link[bixverse:params_scenic]{bixverse::params_scenic()}}.}

\item{wave_byte_budget}{Numeric. VRAM ceiling for per-wave histogram and
cumulative tensors (bytes). Default 4 GiB. Shrink on tight VRAM budgets,
raise on 16 GB+ adapters to keep wave size at 8.}

\item{genes_to_take}{Optional character vector. Target genes to include.
If \code{NULL}, the CPU gene filter runs first.}

\item{cells_to_take}{Optional character vector. Cell names to include. If
\code{NULL}, all filtered cells are used.}

\item{streaming}{Optional boolean. Only used on \code{SingleCells}. If \code{TRUE},
the streaming GPU driver is used (bounded host memory). If \code{NULL}, defaults
to \code{\link[bixverse:::auto_streaming]{bixverse::::auto_streaming()}}. Ignored for \code{MetaCells}.}

\item{random_seed}{Integer. For reproducibility.}

\item{.verbose}{Boolean or integer. Controls verbosity. \code{FALSE} -> quiet,
\code{TRUE} or \code{1L} -> normal, \code{2L} -> detailed.}
}
\value{
A \code{ScenicGrn} object with the gene x TF importance matrix.
}
\description{
GPU counterpart of \code{\link[bixverse:scenic_grn_sc]{bixverse::scenic_grn_sc()}}. Runs multi-output tree
regression on the WGPU backend and returns a \code{ScenicGrn} object. Dispatches
on \code{SingleCells} (disk-backed .bin counts) or \code{MetaCells} (in-memory
sparse) from the \code{bixverse} package. GBM is not supported on GPU; use the
CPU version for \code{learner_type = "gradient_boosting"}.

If \code{genes_to_take} is \code{NULL}, the CPU \code{\link[bixverse:scenic_gene_filter_sc]{bixverse::scenic_gene_filter_sc()}} is
used to shortlist targets (cheap min-counts / min-cells scan).
}
\references{
Aibar et al., Nat Methods, 2017.
}
