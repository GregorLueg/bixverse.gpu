% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_cagra_gpu_knn}
\alias{rs_cagra_gpu_knn}
\title{Generate a CAGRA-style GPU-accelerated kNN graph}
\usage{
rs_cagra_gpu_knn(embd, cagra_params, extract_knn, seed, verbose)
}
\arguments{
\item{embd}{Numeric matrix of embeddings, cells x features.}

\item{cagra_params}{A named list with the parameters, see
\code{\link[=params_sc_cagra]{params_sc_cagra()}}}

\item{extract_knn}{Logical. If \code{TRUE}, extracts the kNN graph directly
from the NNDescent result (faster, slightly lower precision). If
\code{FALSE}, runs beam search over the pruned CAGRA graph (slower, higher
precision).}

\item{seed}{Integer. Random seed for reproducibility.}

\item{verbose}{Logical. Whether to print progress messages.}
}
\value{
A named list with:
\itemize{
\item \code{indices} - Integer matrix of shape cells x k_query with
0-based neighbour indices.
\item \code{dist} - Numeric matrix of shape cells x k_query with distances to
the neighbours.
\item \code{dist_metric} - Character. The distance metric used.
}
}
\description{
Builds a kNN graph from an embedding matrix using the CAGRA algorithm on
the wgpu backend. Supports two retrieval modes: direct extraction from the
NNDescent graph, or beam search over the pruned CAGRA graph.
}
