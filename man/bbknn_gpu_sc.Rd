% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bbknn_gpu.R
\name{bbknn_gpu_sc}
\alias{bbknn_gpu_sc}
\title{Run BBKNN on the GPU}
\usage{
bbknn_gpu_sc(
  object,
  batch_column,
  no_neighbours_to_keep = 5L,
  embd_to_use = "pca",
  no_embd_to_use = NULL,
  bbknn_params = params_sc_bbknn_gpu(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class from \code{bixverse}.}

\item{batch_column}{String. The column with the batch information in the
obs data of the class.}

\item{no_neighbours_to_keep}{Integer. Maximum number of neighbours to keep
from the BBKNN algorithm. Generating neighbours per batch can produce a lot
of them, so this keeps the top \code{no_neighbours_to_keep}. Defaults to \code{5L}.}

\item{embd_to_use}{String. The embedding to use. Atm, the only option is
\code{"pca"}.}

\item{no_embd_to_use}{Optional integer. Number of embedding dimensions to
use. If \code{NULL} all will be used.}

\item{bbknn_params}{List. Output of \code{\link[=params_sc_bbknn_gpu]{params_sc_bbknn_gpu()}}.}

\item{seed}{Integer. Random seed.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run
times. \code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} ->
detailed verbosity.}
}
\value{
The object with the added kNN matrix based on BBKNN and the graph
based on the returned connectivities of the algorithm.
}
\description{
GPU counterpart of \code{\link[bixverse:bbknn_sc]{bixverse::bbknn_sc()}}, implementing the
batch-balanced k-nearest neighbour algorithm from Polański, et al. One
nearest neighbour index is built per batch and queried by every cell, so
each cell gets \code{neighbours_within_batch} neighbours from every batch. The
UMAP connectivity calculations that reduce spurious connections then run on
the CPU, shared with the CPU implementation.
}
\details{
Only the per-batch searches move to the device, and they are the
part that scales with batch count: BBKNN builds one index per batch and
queries each with all cells, so the work grows as \code{n_cells * n_batches}.
With a handful of batches on a small object the CPU is fine. The GPU starts
to matter once you have many samples.

Results match the CPU path exactly with \code{knn_method = "exhaustive"}, since
both are exact and recompute distances against the same embedding. The
approximate backends break ties differently and will not.
}
\references{
Polański, et al., Bioinformatics, 2020
}
