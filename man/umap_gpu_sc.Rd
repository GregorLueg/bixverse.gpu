% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_gpu.R
\name{umap_gpu_sc}
\alias{umap_gpu_sc}
\title{Run UMAP on a SingleCells object (GPU)}
\usage{
umap_gpu_sc(
  object,
  use_knn = TRUE,
  embd_to_use = "pca",
  slot_name = "umap",
  no_embd_to_use = NULL,
  modality = c("rna", "adt", "wnn"),
  n_dim = 2L,
  k = 15L,
  min_dist = 0.5,
  spread = 1,
  knn_method = c("nndescent", "exhaustive", "ivf"),
  nn_params = params_nn_gpu(),
  umap_params = params_umap_gpu(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} (or \code{SingleCellsMultiModal}) class.}

\item{use_knn}{Boolean. Use the kNN graph found in the object. Defaults to
\code{TRUE}. Only reused if the modality lines up; otherwise a fresh GPU kNN is
generated.}

\item{embd_to_use}{String. The embedding to use for UMAP. Must be available
in the object for the chosen modality.}

\item{slot_name}{String. The name of this embedding within the object.
Defaults to \code{"umap"}.}

\item{no_embd_to_use}{Optional integer. Number of embedding dimensions to
use. If \code{NULL}, all will be used.}

\item{modality}{String. On which modality to run UMAP. One of
\code{c("rna", "adt", "wnn")}. The two latter options are only available on
\code{SingleCellsMultiModal}.}

\item{n_dim}{Integer. Number of UMAP dimensions. Defaults to \code{2L}.}

\item{k}{Integer. Number of nearest neighbours. Defaults to \code{15L}.}

\item{min_dist}{Numeric. Minimum distance between embedded points. Defaults
to \code{0.5}.}

\item{spread}{Numeric. Effective scale of embedded points. Defaults to
\code{1.0}.}

\item{knn_method}{String. GPU (approximate) nearest neighbour method. One
of \code{c("nndescent", "exhaustive", "ivf")}.}

\item{nn_params}{Named list. GPU kNN parameters, see \code{\link[=params_nn_gpu]{params_nn_gpu()}}.}

\item{umap_params}{Named list. UMAP (GPU) parameters, see
\code{\link[=params_umap_gpu]{params_umap_gpu()}}.}

\item{seed}{Integer. For reproducibility.}

\item{use_high_precision}{Optional boolean. Fine-grained fp32 vs fp64
control for the optimiser. GPU kNN is always fp32.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
The object with a \code{"umap"} embedding added. If the requested
embedding is missing, returns the object unchanged with a warning.
}
\description{
GPU-accelerated counterpart to \code{\link[bixverse:umap_sc]{bixverse::umap_sc()}}. Pulls an embedding
(defaulting to PCA) off the object, runs \code{\link[=umap_gpu]{umap_gpu()}} on it (GPU kNN plus
GPU Adam optimiser by default), and writes the resulting embedding back
into \code{sc_cache$other_embeddings[[slot_name]]}.

When \code{use_knn = TRUE} (the default), the kNN graph already stored on the
object is reused via \code{\link[bixverse:sc_knn_to_nearest_neighbours]{bixverse::sc_knn_to_nearest_neighbours()}}. Otherwise
a fresh GPU kNN is built from the chosen embedding.
}
\seealso{
\code{\link[=umap_gpu]{umap_gpu()}}, \code{\link[bixverse:umap_sc]{bixverse::umap_sc()}}, \code{\link[=tsne_gpu_sc]{tsne_gpu_sc()}}
}
