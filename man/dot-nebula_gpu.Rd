% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nebula_gpu.R
\name{.nebula_gpu}
\alias{.nebula_gpu}
\title{GPU NEBULA implementation}
\usage{
.nebula_gpu(
  object,
  subject_col,
  design,
  coef,
  contrast,
  genes_to_use,
  offset,
  nebula_params,
  .verbose
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class from \code{bixverse}.}

\item{subject_col}{String. The column in the obs table holding the subject
(donor) identifier. This is what the random effect is over.}

\item{design}{Formula. The experimental design, evaluated against the obs
table, e.g. \code{~ condition} or \code{~ condition + age}. Include the intercept.}

\item{coef}{Optional integer or character. Which coefficient of the design
the Wald test reports, as a 1-based column position or a column name.
Defaults to the last column.}

\item{contrast}{Optional numeric vector. One weight per design column.
Mutually exclusive with \code{coef}.}

\item{genes_to_use}{Optional character vector. The genes to fit. Defaults to
every gene in the object, which is usually too many.}

\item{offset}{Optional numeric vector. Strictly positive scaling factor per
cell, aligned to the cells that survive the design. Defaults to \code{NULL},
which uses the library sizes.}

\item{nebula_params}{A list, see \code{\link[=params_nebula_gpu]{params_nebula_gpu()}}. The list has the
following parameters:
\itemize{
\item nebula_method - String. One of \code{c("ln", "hl")}.
\item min_sigma, max_sigma - Numeric. Bounds on the subject-level
overdispersion.
\item min_phi, max_phi - Numeric. Bounds on the cell-level overdispersion.
\item cutoff_cell - Numeric. When to refit both overdispersions.
\item kappa - Numeric. When to trust the stage-one subject overdispersion.
\item cpc - Numeric. Minimum mean count per cell for a gene to be tested.
\item mincp - Integer. Minimum number of cells expressing a gene.
\item eps - Numeric. Optimiser stopping tolerance.
\item gene_batch_size - Integer. Genes read and fitted per batch.
\item shrink_dispersion - Boolean. Empirical Bayes shrinkage of the
cell-level overdispersions.
}}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run
times. \code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} ->
detailed verbosity.}
}
\value{
A \code{ScNebula} class.
}
\description{
Shared body of the \code{\link[=nebula_gpu_sc]{nebula_gpu_sc()}} methods. Mirrors the CPU
\code{bixverse::nebula_sc()} method step for step and only swaps the Rust call.
}
\keyword{internal}
