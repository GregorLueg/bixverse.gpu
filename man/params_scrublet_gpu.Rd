% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_scrublet_gpu}
\alias{params_scrublet_gpu}
\title{Wrapper function for GPU Scrublet doublet detection parameters}
\usage{
params_scrublet_gpu(
  sim_doublet_ratio = 1.5,
  expected_doublet_rate = 0.1,
  stdev_doublet_rate = 0.02,
  n_bins_histogram = 100L,
  manual_threshold = NULL,
  no_pcs = 30L,
  normalisation = list(),
  hvg = list(),
  knn_backend = c("gpu", "cpu"),
  knn = list(k = 0L)
)
}
\arguments{
\item{sim_doublet_ratio}{Numeric. Number of doublets to simulate relative to
the number of observed cells. Defaults to \code{1.5}.}

\item{expected_doublet_rate}{Numeric in \verb{[0, 1]}. Expected doublet rate,
typically 0.05-0.10 depending on cell loading. Defaults to \code{0.1}.}

\item{stdev_doublet_rate}{Numeric in \verb{[0, 1]}. Uncertainty in the expected
doublet rate. Defaults to \code{0.02}.}

\item{n_bins_histogram}{Integer. Histogram bins for the Otsu threshold
search. Defaults to \code{100L}.}

\item{manual_threshold}{Optional numeric. Fixed doublet score threshold. If
\code{NULL}, Otsu's method picks it.}

\item{no_pcs}{Integer. Number of principal components. Defaults to \code{30L}.}

\item{normalisation}{List. Optional overrides. See
\code{\link[bixverse:params_norm_doublets_defaults]{bixverse::params_norm_doublets_defaults()}} for the available parameters:
\code{log_transform}, \code{mean_center}, \code{normalise_variance}, \code{target_size}.}

\item{hvg}{List. Optional overrides. See \code{\link[bixverse:params_hvg_defaults]{bixverse::params_hvg_defaults()}}
for the available parameters: \code{min_gene_var_pctl}, \code{hvg_method},
\code{loess_span}, \code{clip_max}, \code{n_bins}, \code{binning_strategy}.}

\item{knn_backend}{String. One of \code{"gpu"} or \code{"cpu"}. Picks which nearest
neighbour index runs over the combined observed-plus-simulated embedding,
and with it which keys \code{knn} accepts. \code{"gpu"} is the fast default; \code{"cpu"}
buys the exact CPU indices at the cost of a host round trip on a matrix
that is \code{(1 + sim_doublet_ratio) * n_cells} rows tall.}

\item{knn}{List. Optional overrides for the kNN block. Validated against
\code{\link[=params_knn_gpu_defaults]{params_knn_gpu_defaults()}} when \code{knn_backend = "gpu"} and against
\code{\link[bixverse:params_knn_defaults]{bixverse::params_knn_defaults()}} when \code{knn_backend = "cpu"}. Unknown keys
are an error, not a silent pass-through. Defaults to \code{list(k = 0L)}, which
asks Rust to pick \code{k}.}
}
\value{
A flat named list with all GPU Scrublet parameters.
}
\description{
GPU counterpart to \code{\link[bixverse:params_scrublet]{bixverse::params_scrublet()}}. Two
differences from the CPU list. The \code{pca} sub-list is gone: the GPU SVD is
always randomised, so \code{random_svd} and \code{sparse} have nothing to switch and
\code{no_pcs} is a plain argument. And the kNN block is backend dependent, see
\code{knn_backend}.
}
\references{
Wolock, et al., Cell Syst, 2020
}
