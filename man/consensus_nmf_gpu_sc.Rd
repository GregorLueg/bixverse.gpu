% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmf_gpu.R
\name{consensus_nmf_gpu_sc}
\alias{consensus_nmf_gpu_sc}
\title{Run consensus NMF on the GPU over single cell or meta cell data}
\usage{
consensus_nmf_gpu_sc(
  object,
  k,
  cell_ids = NULL,
  gene_ids = NULL,
  preprocessing = "none",
  use_second_layer = TRUE,
  nmf_hals_params = bixverse::params_nmf_hals(),
  nmf_consensus_params = bixverse::params_nmf_consensus(),
  n_runs = 30L,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{MetaCells} class from \code{bixverse}.}

\item{k}{Integer. Number of latent factors. At least 2, at most 128, see
\link{NMF_GPU_MAX_RANK}.}

\item{cell_ids}{Optional character. Cell ids (or meta cell ids) to restrict
the NMF to. If \code{NULL}, uses \code{\link[bixverse:get_cells_to_keep]{bixverse::get_cells_to_keep()}} for \code{SingleCells}
and all meta cells for \code{MetaCells}.}

\item{gene_ids}{Optional character. Gene ids to restrict the NMF to. If
\code{NULL}, uses \code{\link[bixverse:get_hvg]{bixverse::get_hvg()}} on the object.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on the normalised
counts (recommended); if \code{FALSE}, on the raw counts.}

\item{nmf_hals_params}{List, see \code{\link[bixverse:params_nmf_hals]{bixverse::params_nmf_hals()}}. The
\code{nmf_init} field is ignored, restarts always use random initialisation.}

\item{nmf_consensus_params}{List, see \code{\link[bixverse:params_nmf_consensus]{bixverse::params_nmf_consensus()}}.}

\item{n_runs}{Integer. Number of restarts. At least 2.}

\item{seed}{Integer. Random seed for initialisation.}

\item{.verbose}{Boolean or integer. Controls verbosity. \code{FALSE} -> quiet,
\code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed verbosity.}
}
\value{
A \code{ConsensusNmfResult} object, the same class
\code{\link[bixverse:consensus_nmf_sc]{bixverse::consensus_nmf_sc()}} returns.
}
\description{
GPU counterpart of \code{\link[bixverse:consensus_nmf_sc]{bixverse::consensus_nmf_sc()}}. Runs \code{n_runs} HALS
restarts on the device, pools their components, drops unstable ones by local
density, k-means clusters the survivors and refits the partner factor against
the per-cluster median.

Prefer this over \code{\link[=stabilised_nmf_gpu_sc]{stabilised_nmf_gpu_sc()}}, which picks the lowest-loss
restart. Use \code{\link[=nmf_k_sweep_gpu_sc]{nmf_k_sweep_gpu_sc()}} first if you do not already know \code{k}.
}
\details{
Only the restarts move to the GPU. The pooling, the density filter, the
k-means and the silhouette all run on the CPU, shared with the bixverse
implementation, so the speed-up tracks how much of the run the solves own.

The restart factors are dense and all held at once, so budget for \code{n_runs}
times \code{k} times the cell count on top of the counts themselves.

If the density filter leaves fewer than \code{k} components, or a cluster comes
out empty, the run errors rather than returning a partial answer. Raise
\code{density_threshold} (2 switches the filter off) or increase \code{n_runs}.
}
\references{
Kotliar et al., eLife, 2019
}
