% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.manifold_from_gpu}
\alias{.manifold_from_gpu}
\title{Parents of a GPU manifold embedding (UMAP, t-SNE)}
\usage{
.manifold_from_gpu(embd_to_use, cache_modality, modality, has_knn)
}
\arguments{
\item{embd_to_use}{String. Name of the source embedding.}

\item{cache_modality}{String. Modality the source embedding was read from.}

\item{modality}{String. Modality the result is written to.}

\item{has_knn}{Boolean. Whether a cached kNN fed the manifold.}
}
\value{
Character vector of parent artefact names.
}
\description{
Builds the \code{from} argument for \code{bixverse::set_embedding()} so the resulting
embedding joins the provenance chain rather than being recorded as a root.
The source embedding is read from \code{cache_modality} while the result is
written under \code{modality}, and the two differ for \code{"wnn"}, so both parents
are spelled out modality qualified.

Kept local rather than reaching for the equivalent \code{bixverse} internal: four
lines are not worth coupling to an unexported function.
}
\keyword{internal}
