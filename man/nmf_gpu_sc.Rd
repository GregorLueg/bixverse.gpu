% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmf_gpu.R
\name{nmf_gpu_sc}
\alias{nmf_gpu_sc}
\title{Run single-run NMF on the GPU over single cell or meta cell data}
\usage{
nmf_gpu_sc(
  object,
  k,
  cell_ids = NULL,
  gene_ids = NULL,
  preprocessing = "none",
  use_second_layer = TRUE,
  nmf_hals_params = bixverse::params_nmf_hals(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{MetaCells} class from \code{bixverse}.}

\item{k}{Integer. Number of latent factors to return. At most 128, see
\link{NMF_GPU_MAX_RANK}.}

\item{cell_ids}{Optional character. Cell ids (or meta cell ids) to restrict
the NMF to. If \code{NULL}, uses \code{\link[bixverse:get_cells_to_keep]{bixverse::get_cells_to_keep()}} for \code{SingleCells}
and all meta cells for \code{MetaCells}.}

\item{gene_ids}{Optional character. Gene ids to restrict the NMF to. If
\code{NULL}, uses \code{\link[bixverse:get_hvg]{bixverse::get_hvg()}} on the object.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on the normalised
counts (recommended); if \code{FALSE}, on the raw counts.}

\item{nmf_hals_params}{List, see \code{\link[bixverse:params_nmf_hals]{bixverse::params_nmf_hals()}}.}

\item{seed}{Integer. Random seed for initialisation.}

\item{.verbose}{Boolean or integer. Controls verbosity. \code{FALSE} -> quiet,
\code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed verbosity.}
}
\value{
An \code{NmfResult} object, the same class \code{\link[bixverse:nmf_sc]{bixverse::nmf_sc()}} returns.
}
\description{
GPU counterpart of \code{\link[bixverse:nmf_sc]{bixverse::nmf_sc()}}. Runs one HALS NMF on a chosen subset
of cells and genes. The counts are uploaded once and the whole HALS loop runs
on the WGPU backend; the NNDSVD initialisation stays on the CPU.

For \code{SingleCells} the counts are streamed from the Rust binary files, for
\code{MetaCells} the in-memory sparse counts are used. Params, result class and
downstream code are identical to the CPU version.

A single run is here for parity rather than speed. The GPU pays off when the
same matrix serves many solves, so reach for \code{\link[=consensus_nmf_gpu_sc]{consensus_nmf_gpu_sc()}} or
\code{\link[=nmf_k_sweep_gpu_sc]{nmf_k_sweep_gpu_sc()}} if you want the speed-up.
}
