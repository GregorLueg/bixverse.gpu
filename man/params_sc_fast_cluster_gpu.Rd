% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sc_fast_cluster_gpu}
\alias{params_sc_fast_cluster_gpu}
\title{Default parameters for GPU fast Louvain clustering}
\usage{
params_sc_fast_cluster_gpu(
  k_means_iter = 50L,
  k_means_init = NULL,
  fixed = TRUE,
  quantise = FALSE,
  same_weight = FALSE,
  full_snn = FALSE,
  pruning = NULL,
  snn_similarity = c("jaccard", "rank"),
  louvain_iters = 10L,
  multi_level_louvain = TRUE,
  knn = list(k = 5L)
)
}
\arguments{
\item{k_means_iter}{Integer. Maximum number of k-means iterations.}

\item{k_means_init}{Optional character. Initialisation method. One of
\code{"random"}, \code{"parallel"} or \code{"plusplus"}. If \code{NULL}, picked on the Rust side
based on the number of centroids.}

\item{fixed}{Boolean. Shall k-means run for a fixed number of iterations,
without checking for convergence.}

\item{quantise}{Boolean. Shall the data buffer be held at fp16 on the GPU.
Halves the buffer and helps when the assignment kernels are memory bound.}

\item{same_weight}{Boolean. If \code{TRUE}, all kNN edges get weight \code{1.0}.
Otherwise edges with a reverse counterpart are double counted.}

\item{full_snn}{Boolean. Shall the full shared nearest neighbour graph be
generated, including edges between centroids that are not neighbours.}

\item{pruning}{Optional numeric. Weights below this threshold are set to 0
when generating the sNN graph. If \code{NULL}, defaults to \code{1 / ceiling(k * 0.8)}.}

\item{snn_similarity}{String. One of \code{c("jaccard", "rank")}. Jaccard
computes the Jaccard index between neighbour sets; rank weights edges by the
best combined rank of a shared neighbour. Both are normalised to \verb{[0, 1]}.}

\item{louvain_iters}{Integer. Number of Louvain iterations.}

\item{multi_level_louvain}{Boolean. Shall multi-level Louvain be applied.}

\item{knn}{List. Optional overrides for the kNN parameters applied to the
centroids. See \code{\link[bixverse:params_knn_defaults]{bixverse::params_knn_defaults()}} for the available
parameters. Defaults to \code{k = 5L}.}
}
\value{
A named list with the GPU fast clustering parameters.
}
\description{
GPU counterpart to \code{\link[bixverse:params_sc_fast_cluster]{bixverse::params_sc_fast_cluster()}}. The mini-batch
k-means knobs are gone (the GPU k-means is full-batch Lloyd's) and the
k-means block comes from the GPU parameters instead. Two knobs the CPU
wrapper never exposed, \code{same_weight} and \code{multi_level_louvain}, are
available here.

The k-means distance is taken from \code{knn$ann_dist}, so the coarsening and the
centroid graph agree on the geometry. There is no separate \code{metric}
argument, and \code{"manhattan"} is not supported by the GPU k-means.
}
