% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_gpu.R
\name{generate_cagra_knn_sc}
\alias{generate_cagra_knn_sc}
\title{Generate CAGRA GPU kNN data for single cells}
\usage{
generate_cagra_knn_sc(
  object,
  embd_to_use = "pca",
  cells_to_use = NULL,
  no_embd_to_use = NULL,
  modality = c("rna", "adt"),
  cagra_params = params_sc_cagra(),
  extract_knn = TRUE,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} (or \code{SingleCellsMultiModal}) class.}

\item{embd_to_use}{String. The embedding to use. Whichever you choose, it
needs to be part of the object for the selected modality.}

\item{cells_to_use}{Optional string vector. Cell names to include. If \code{NULL}
all cells in the object will be used.}

\item{no_embd_to_use}{Optional integer. Number of embedding dimensions to
use. If \code{NULL} all will be used.}

\item{modality}{String. One of \code{c("rna", "adt")}. You can only use \code{"adt"}
on \code{SingleCellsMultiModal} class.}

\item{cagra_params}{List. Output of \code{\link[=params_sc_cagra]{params_sc_cagra()}}.}

\item{extract_knn}{Logical. If \code{TRUE}, extracts the kNN graph directly from
the NNDescent result (faster, slightly lower precision). If \code{FALSE}, runs
beam search over the pruned CAGRA graph (slower, higher precision).}

\item{seed}{Integer. For reproducibility.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
Initialised \code{sc_knn} with the kNN data.
}
\description{
This function generates a \code{SingleCellNearestNeighbour} object using the
CAGRA (CUDA-Accelerated Graph Retrieval Approximation) algorithm via the
\code{bixverse.gpu} package. CAGRA first builds a dense NNDescent graph, then
prunes it into a sparser navigational graph optimised for beam-search
traversal. Two retrieval modes are available: direct extraction from the
NNDescent graph (\code{extract_knn = TRUE}), which is faster but slightly less
precise, or beam search over the pruned CAGRA graph (\code{extract_knn = FALSE}),
which is slower but yields higher recall. This function is the CAGRA
counterpart of \code{\link[bixverse:generate_knn_sc]{generate_knn_sc()}}.
}
