% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_nebula_gpu}
\alias{params_nebula_gpu}
\title{Wrapper function for parameters for GPU NEBULA}
\usage{
params_nebula_gpu(
  nebula_method = c("ln", "hl"),
  min_sigma = 1e-04,
  min_phi = 1e-04,
  max_sigma = 10,
  max_phi = 1000,
  cutoff_cell = 20,
  kappa = 800,
  cpc = 0.005,
  mincp = 5L,
  eps = 1e-06,
  gene_batch_size = 1000L,
  shrink_dispersion = TRUE
)
}
\arguments{
\item{nebula_method}{String. Which variant to run. NEBULA downgrades \code{"ln"}
to \code{"hl"} below 30 cells per subject, as the R package does. One of \code{c("ln", "hl")}. Defaults to \code{"ln"}.}

\item{min_sigma}{Numeric. Lower bound on the subject-level overdispersion.
Defaults to \code{1e-04}.}

\item{min_phi}{Numeric. Lower bound on the cell-level overdispersion.
Defaults to \code{1e-04}.}

\item{max_sigma}{Numeric. Upper bound on the subject-level overdispersion.
Defaults to \code{10.0}.}

\item{max_phi}{Numeric. Upper bound on the cell-level overdispersion.
Defaults to \code{1000.0}.}

\item{cutoff_cell}{Numeric. Refit both overdispersions when the product of
the cells per subject and the estimated \code{phi} falls below this. Defaults to
\code{20.0}.}

\item{kappa}{Numeric. Threshold on NEBULA's \code{kappa_obs} above which the
subject-level overdispersion from stage one is trusted as is. Defaults to
\code{800.0}.}

\item{cpc}{Numeric. Drop a gene whose mean count per cell is at most this.
Defaults to \code{0.005}.}

\item{mincp}{Integer. Drop a gene expressed in fewer than this many cells.
Defaults to \code{5L}.}

\item{eps}{Numeric. Absolute stopping tolerance for the optimiser. Defaults
to \code{1e-06}.}

\item{gene_batch_size}{Integer. Genes read and fitted per batch. Bounds how
much of the store is resident at once and changes nothing about the answer,
since NEBULA is gene-independent. Defaults to \code{1000L}.}

\item{shrink_dispersion}{Boolean. Shrink the cell-level overdispersions
towards an empirical Bayes prior once the sweep is done. Defaults to \code{TRUE}.}
}
\value{
A named list with the following elements:
\itemize{
\item nebula_method - String. Which variant to run. NEBULA downgrades \code{"ln"}
to \code{"hl"} below 30 cells per subject, as the R package does. One of \code{c("ln", "hl")}. Defaults to \code{"ln"}.
\item min_sigma - Numeric. Lower bound on the subject-level overdispersion.
Defaults to \code{1e-04}.
\item min_phi - Numeric. Lower bound on the cell-level overdispersion.
Defaults to \code{1e-04}.
\item max_sigma - Numeric. Upper bound on the subject-level overdispersion.
Defaults to \code{10.0}.
\item max_phi - Numeric. Upper bound on the cell-level overdispersion.
Defaults to \code{1000.0}.
\item cutoff_cell - Numeric. Refit both overdispersions when the product of
the cells per subject and the estimated \code{phi} falls below this. Defaults to
\code{20.0}.
\item kappa - Numeric. Threshold on NEBULA's \code{kappa_obs} above which the
subject-level overdispersion from stage one is trusted as is. Defaults to
\code{800.0}.
\item cpc - Numeric. Drop a gene whose mean count per cell is at most this.
Defaults to \code{0.005}.
\item mincp - Integer. Drop a gene expressed in fewer than this many cells.
Defaults to \code{5L}.
\item eps - Numeric. Absolute stopping tolerance for the optimiser. Defaults
to \code{1e-06}.
\item gene_batch_size - Integer. Genes read and fitted per batch. Bounds how
much of the store is resident at once and changes nothing about the answer,
since NEBULA is gene-independent. Defaults to \code{1000L}.
\item shrink_dispersion - Boolean. Shrink the cell-level overdispersions
towards an empirical Bayes prior once the sweep is done. Defaults to \code{TRUE}.
}
}
\description{
GPU counterpart to \code{\link[bixverse:params_nebula]{bixverse::params_nebula()}}. Same knobs and
defaults, minus \code{reml}: the device fit does not implement it. Stage two of
NEBULA, the per-gene penalised fits, runs in \code{f32} on the device and is
finished on the host in \code{f64}, so the estimates sit close to the CPU ones
rather than on them.
}
\references{
He, et al., Commun Biol, 2021
}
