% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seacells_gpu.R
\name{generate_seacells_gpu_sc}
\alias{generate_seacells_gpu_sc}
\title{Generate meta cells based on SEACells on the GPU}
\usage{
generate_seacells_gpu_sc(
  object,
  seacell_params = bixverse::params_sc_seacells(),
  embd_to_use = "pca",
  no_embd_to_use = NULL,
  cells_to_use = NULL,
  regenerate_knn = FALSE,
  target_size = 1e+05,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} or \code{SingleCellsSubset} class from \code{bixverse}.}

\item{seacell_params}{List. Output of \code{\link[bixverse:params_sc_seacells]{bixverse::params_sc_seacells()}}.
A list with the following items:
\itemize{
\item n_sea_cells - Number of SEA cells to detect.
\item max_fw_iters - Maximum iterations for the Franke-Wolfe algorithm per
matrix update.
\item convergence_epsilon - Convergence threshold. Algorithm stops when
RSS change < epsilon * RSS(0).
\item max_iter - Maximum iterations to run SEACells for.
\item min_iter - Minimum iterations to run SEACells for.
\item greedy_threshold - Maximum number of cells before defaulting to rapid
random selection of archetypes.
\item graph_building - Graph building method.
\item pruning - Boolean. Shall small values be pruned during the Franke-
Wolfe iterations.
\item pruning_threshold - The threshold below which pruning shall be
applied during Franke-Wolfe iterations.
\item n_landmarks - Optional integer. Number of landmarks for the Nystroem
archetype initialisation.
\item knn - List of kNN parameters. See \code{\link[bixverse:params_knn_defaults]{bixverse::params_knn_defaults()}}
for available parameters and their defaults.
}}

\item{embd_to_use}{String. The embedding to use. Atm, the only option is
\code{"pca"}.}

\item{no_embd_to_use}{Optional integer. Number of embedding dimensions to
use. If \code{NULL} all will be used.}

\item{cells_to_use}{Optional string. Names of the cells to use for the
generation of the SEACells. Forces a kNN rebuild on that subset.}

\item{regenerate_knn}{Boolean. Shall a kNN graph be regenerated. If not,
the internal one will be used.}

\item{target_size}{Numeric. The library target size to normalise the meta
cells to.}

\item{seed}{Integer. Seed for reproducibility.}

\item{.verbose}{Boolean or integer. Controls verbosity and returns run times.
\code{FALSE} -> quiet, \code{TRUE} or \code{1L} -> normal verbosity, \code{2L} -> detailed
verbosity.}
}
\value{
A \code{\link[bixverse:MetaCells]{bixverse::MetaCells()}} with the data generated by this meta cell
aggregation method.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
GPU counterpart of \code{\link[bixverse:generate_seacells_sc]{bixverse::generate_seacells_sc()}}. The Frank-Wolfe
B-gradient argmin, which dominates the runtime, runs on the WGPU backend.
Kernel construction, archetype initialisation, the A update and the
aggregation into pseudo-bulk counts stay on the CPU, so the speedup tracks
how much of the run the argmin owns. That share grows with \code{n_sea_cells}.

Params, result class and downstream code are identical to the CPU version.
}
\references{
Persad, et al. Nat Biotechnol, 2023
}
