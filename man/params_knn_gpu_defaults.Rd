% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_knn_gpu_defaults}
\alias{params_knn_gpu_defaults}
\title{Default parameters for the GPU nearest neighbour backends}
\usage{
params_knn_gpu_defaults()
}
\value{
A named list with the following elements:
\itemize{
\item k - Integer. Number of neighbours. \code{0L} hands the choice to Rust,
which uses \code{sqrt(n_cells) * 0.5} and then adjusts for the simulated
doublets. Defaults to \code{15L}.
\item knn_method - String. The GPU index to use. One of \code{c("exhaustive", "ivf", "nndescent")}. Defaults to \code{"exhaustive"}.
\item ann_dist - String. Manhattan is not supported by the GPU kernels. One
of \code{c("euclidean", "cosine")}. Defaults to \code{"euclidean"}.
\item n_list - Integer or \code{NULL}. IVF only. Number of clusters. \code{NULL} gives
\code{sqrt(n)}. Defaults to \code{NULL}.
\item n_probe - Integer or \code{NULL}. IVF only. Clusters to probe. \code{NULL} gives
\code{sqrt(n_list)}. Defaults to \code{NULL}.
\item graph_k - Integer or \code{NULL}. NN-descent only. Node degree of the graph
after pruning. \code{NULL} gives 30, widened to cover \code{k} when \code{extract_knn} is
set. Defaults to \code{NULL}.
\item k_build - Integer or \code{NULL}. NN-descent only. Build degree before
pruning. \code{NULL} gives \code{max(k, floor(1.5 * k))}. Defaults to \code{NULL}.
\item n_tree - Integer or \code{NULL}. NN-descent only. Trees seeding the
descent. Defaults to \code{NULL}.
\item delta - Numeric. NN-descent only. Termination criterium for the
descent. Defaults to \code{0.001}.
\item rho - Numeric or \code{NULL}. NN-descent only. Sampling rate for the
descent. Defaults to \code{NULL}.
\item refine_knn - Integer or \code{NULL}. NN-descent only. 2-hop refinement
sweeps after the descent. Buys graph quality at a linear cost. \code{NULL} gives
0. Defaults to \code{NULL}.
\item beam_width - Integer or \code{NULL}. NN-descent only. Beam width when
querying. Ignored when \code{extract_knn} is set. Defaults to \code{NULL}.
\item max_beam_iters - Integer or \code{NULL}. NN-descent only. Beam search
iterations. Ignored when \code{extract_knn} is set. Defaults to \code{NULL}.
\item n_entry_points - Integer or \code{NULL}. NN-descent only. Entry points when
querying. Ignored when \code{extract_knn} is set. Defaults to \code{NULL}.
\item extract_knn - Boolean. NN-descent only. Hand back the graph the
descent built instead of beam searching over it. Defaults to \code{FALSE}.
}
}
\description{
GPU sibling of \code{\link[bixverse:params_knn_defaults]{bixverse::params_knn_defaults()}}. The GPU
indices take a different knob set: there is no Annoy and no HNSW on the
device, so what survives is exhaustive, IVF and NN-descent.
}
\details{
NN-descent builds a CAGRA graph and, with \code{extract_knn = TRUE}, hands that
graph back rather than beam searching over it. Note that this saves the
query, not the build: the descent itself dominates, and its build degree
tracks \code{k}. NN-descent is therefore a low-\code{k} tool on the GPU. Above \code{k} of
roughly 30 both exhaustive and IVF beat it, and by \code{k = 200} they beat it by
more than an order of magnitude.
}
