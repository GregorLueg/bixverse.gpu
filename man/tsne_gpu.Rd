% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tsne_gpu.R
\name{tsne_gpu}
\alias{tsne_gpu}
\title{Rust-based t-SNE (GPU)}
\usage{
tsne_gpu(
  data,
  knn = NULL,
  n_dim = 2L,
  perplexity = 20,
  approx_type = c("bh", "fft"),
  knn_method = c("nndescent", "exhaustive", "ivf"),
  nn_params = params_nn_gpu(),
  tsne_params = params_tsne_gpu(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to embed of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, t-SNE will skip
the k-nearest neighbour graph generation and use this one. Defaults to
\code{NULL}. See \code{\link[manifoldsR:new_nearest_neighbour]{manifoldsR::new_nearest_neighbour()}} for details.}

\item{n_dim}{Integer. Number of dimensions in the embedding space.
Currently only \code{2L} is supported. Defaults to \code{2L}.}

\item{perplexity}{Numeric. Perplexity parameter, related to the number of
nearest neighbours used in manifold learning. Typical values are between
5 and 50. Defaults to \code{20.0}.}

\item{approx_type}{Character. Approximation method for computing repulsive
forces. One of \code{"bh"} for Barnes-Hut or \code{"fft"} for FFT-accelerated
interpolation. Defaults to \code{"bh"}. The FFT variant is only available on
Unix systems.}

\item{knn_method}{Character. GPU-accelerated (approximate) nearest
neighbour method to use. One of \code{"nndescent"}, \code{"exhaustive"}, or \code{"ivf"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn_gpu]{params_nn_gpu()}}.}

\item{tsne_params}{Named list. t-SNE (GPU) algorithm parameters, see
\code{\link[=params_tsne_gpu]{params_tsne_gpu()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{use_high_precision}{Optional boolean. Gives fine-grained control over
\code{fp32} vs \code{fp64} usage. The GPU kNN calculations will be forced into \code{fp32}.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions samples x n_dim containing
the t-SNE embedding.
}
\description{
Performs t-SNE dimensionality reduction on the input data.
This function provides a user-friendly interface with input validation
before calling the Rust implementation. Leverages GPU-accelerated kNN
searches. The optimisation itself still runs on the CPU (a GPU optimiser
is on the roadmap).
}
\details{
The number of neighbours is derived from \code{perplexity} on the Rust
side following the usual \code{3 * perplexity} convention.
}
