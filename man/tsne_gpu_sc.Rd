% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_gpu.R
\name{tsne_gpu_sc}
\alias{tsne_gpu_sc}
\title{Run t-SNE on a SingleCells object (GPU)}
\usage{
tsne_gpu_sc(
  object,
  use_knn = FALSE,
  embd_to_use = "pca",
  slot_name = "tsne",
  no_embd_to_use = NULL,
  modality = c("rna", "adt", "wnn"),
  n_dim = 2L,
  perplexity = 20,
  approx_type = c("bh", "fft"),
  knn_method = c("nndescent", "exhaustive", "ivf"),
  nn_params = params_nn_gpu(),
  tsne_params = params_tsne_gpu(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} (or \code{SingleCellsMultiModal}) class.}

\item{use_knn}{Boolean. Defaults to \code{FALSE}. Set to \code{TRUE} to reuse the
cached kNN; only sensible when the stored \code{k} is at least
\code{3 * perplexity}.}

\item{embd_to_use}{String. The embedding to use for t-SNE. Must be
available in the object for the chosen modality.}

\item{slot_name}{String. The name of this embedding within the object.
Defaults to \code{"tsne"}.}

\item{no_embd_to_use}{Optional integer. Number of embedding dimensions to
use. If \code{NULL}, all will be used.}

\item{modality}{String. On which modality to run t-SNE. One of
\code{c("rna", "adt", "wnn")}. The two latter options are only available on
\code{SingleCellsMultiModal}.}

\item{n_dim}{Integer. Number of t-SNE dimensions. Currently only \code{2L} is
supported. Defaults to \code{2L}.}

\item{perplexity}{Numeric. Perplexity parameter. Typical values between 5
and 50. Defaults to \code{20.0}.}

\item{approx_type}{String. Approximation method. One of \code{"bh"}
(Barnes-Hut) or \code{"fft"}. Defaults to \code{"bh"}. \code{"fft"} is Unix-only.}

\item{knn_method}{String. GPU (approximate) nearest neighbour method. One
of \code{c("nndescent", "exhaustive", "ivf")}.}

\item{nn_params}{Named list. GPU kNN parameters, see \code{\link[=params_nn_gpu]{params_nn_gpu()}}.}

\item{tsne_params}{Named list. t-SNE (GPU) parameters, see
\code{\link[=params_tsne_gpu]{params_tsne_gpu()}}.}

\item{seed}{Integer. For reproducibility.}

\item{use_high_precision}{Optional boolean. Fine-grained fp32 vs fp64
control. GPU kNN is always fp32.}

\item{.verbose}{Boolean or integer. Controls verbosity.}
}
\value{
The object with a \code{"tsne"} embedding added. If the requested
embedding is missing, returns the object unchanged with a warning.
}
\description{
GPU-accelerated counterpart to \code{\link[bixverse:tsne_sc]{bixverse::tsne_sc()}}. Runs \code{\link[=tsne_gpu]{tsne_gpu()}} on
an embedding pulled from the object; only the kNN step is GPU-accelerated,
the optimiser still runs on CPU (a GPU optimiser is on the roadmap).

t-SNE derives the number of neighbours from \code{perplexity} on the Rust side
(the usual \code{3 * perplexity} convention). To avoid a silent mismatch with
the cached kNN, \code{use_knn} defaults to \code{FALSE}: every call generates a
fresh GPU kNN sized to the requested perplexity. Handy for sweeping
perplexities since the kNN is cheap on GPU.
}
\seealso{
\code{\link[=tsne_gpu]{tsne_gpu()}}, \code{\link[bixverse:tsne_sc]{bixverse::tsne_sc()}}, \code{\link[=umap_gpu_sc]{umap_gpu_sc()}}
}
