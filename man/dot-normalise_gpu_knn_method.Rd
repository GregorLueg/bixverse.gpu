% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.normalise_gpu_knn_method}
\alias{.normalise_gpu_knn_method}
\title{Normalise a GPU kNN method name}
\usage{
.normalise_gpu_knn_method(x)
}
\arguments{
\item{x}{String. The kNN method name.}
}
\value{
The string Rust understands.
}
\description{
The package says \code{"nndescent"} everywhere, but the parser on the Rust side
of the single cell GPU kNN only matches \code{"cagra"}, \code{"cagra_gpu"} and
\code{"nndescent_gpu"}. A bare \code{"nndescent"} is not an error there: it prints a
warning and silently runs exhaustive instead. That is a performance
regression nobody would notice, so translate before crossing over.

Idempotent, so it is safe to apply both in the parameter wrapper and again
at the call site.
}
\keyword{internal}
