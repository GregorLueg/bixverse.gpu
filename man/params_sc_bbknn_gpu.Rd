% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_wrappers.R
\name{params_sc_bbknn_gpu}
\alias{params_sc_bbknn_gpu}
\title{Wrapper function for the GPU BBKNN parameters}
\usage{
params_sc_bbknn_gpu(
  neighbours_within_batch = 3L,
  set_op_mix_ratio = 1,
  local_connectivity = 1,
  trim = NULL,
  knn = list()
)
}
\arguments{
\item{neighbours_within_batch}{Integer. Number of neighbours to consider
per batch. Defaults to \code{3L}.}

\item{set_op_mix_ratio}{Numeric. Mixing ratio between union (1.0) and
intersection (0.0). Defaults to \code{1.0}.}

\item{local_connectivity}{Numeric. UMAP connectivity computation parameter,
how many nearest neighbours of each cell are assumed to be fully connected.
Defaults to \code{1.0}.}

\item{trim}{Optional integer. Trim the neighbours of each cell to these many
top connectivities. May help with population independence and improve the
tidiness of clustering. If \code{NULL}, it defaults to
\code{10 * neighbours_within_batch}.}

\item{knn}{List. Optional overrides for the kNN block. Validated against
\code{\link[=params_knn_gpu_defaults]{params_knn_gpu_defaults()}} minus \code{k} and \code{extract_knn}. Unknown keys are
an error, not a silent pass-through.}
}
\value{
A flat named list with all GPU BBKNN parameters.
}
\description{
GPU counterpart to \code{\link[bixverse:params_sc_bbknn]{bixverse::params_sc_bbknn()}}. Same BBKNN
knobs, but the kNN block is the GPU one, see \code{\link[=params_knn_gpu_defaults]{params_knn_gpu_defaults()}}.
}
\details{
Two keys of the GPU kNN block do nothing here and are rejected
rather than silently ignored. \code{k} is set by \code{neighbours_within_batch}, and
\code{extract_knn} only applies to a self-query, whereas BBKNN builds one index
per batch and queries each with every cell.
}
\references{
Polański, et al., Bioinformatics, 2020
}
