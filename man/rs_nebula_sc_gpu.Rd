% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_nebula_sc_gpu}
\alias{rs_nebula_sc_gpu}
\title{GPU: fit the NEBULA negative binomial gamma mixed model over single cells}
\usage{
rs_nebula_sc_gpu(
  f_path_genes,
  f_path_cells,
  cells_to_keep,
  gene_indices,
  subject_ids,
  design,
  offset,
  nebula_params,
  verbose
)
}
\arguments{
\item{f_path_genes}{String. Path to the \code{counts_genes.bin} file.}

\item{f_path_cells}{String. Path to the \code{counts_cells.bin} file. Only read
when \code{offset} is \code{NULL}, to take the library sizes.}

\item{cells_to_keep}{Integer vector. 0-indexed(!) global positions of the
cells to analyse, in any order. Must not hold duplicates.}

\item{gene_indices}{Integer vector. 0-indexed(!) positions of the genes to
fit.}

\item{subject_ids}{Integer vector. 0-indexed(!) subject label per global
cell. One entry per cell in the store, not per cell in \code{cells_to_keep}.}

\item{design}{Numeric matrix. Predictors of cells x coefficients, rows
aligned to \code{cells_to_keep} and including an intercept.}

\item{offset}{Optional numeric vector. Strictly positive scaling factor per
selected cell, aligned to \code{cells_to_keep}. \code{NULL} uses the library sizes.}

\item{nebula_params}{Named list. The NEBULA parameters, see
\code{\link[=params_nebula_gpu]{params_nebula_gpu()}}, plus either \code{coef} (a 0-indexed(!) coefficient) or
\code{contrast} (one weight per coefficient).}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list with the following elements
\itemize{
\item gene_idx - Integer. 0-indexed positions of the genes that survived
NEBULA's own expression filter.
\item coefficients - Numeric matrix of genes x coefficients. The fixed
effects on the design scale.
\item se - Numeric matrix of genes x coefficients. The standard errors.
\item subject_overdispersion - Numeric. NEBULA's \code{sigma^2}.
\item cell_overdispersion - Numeric. NEBULA's \code{phi^-1}.
\item cell_overdispersion_shrunk - Numeric or \code{NULL}. The cell-level
overdispersion after empirical Bayes shrinkage, when it was requested.
\item convergence - Integer. NEBULA's convergence code. At or below \code{-20}
is a likely failure.
\item sigma_at_bound - Boolean. Whether the subject-level variance
finished pinned on its lower bound.
\item log_fc - Numeric. Effect of the tested coefficient or contrast, on
the natural log scale.
\item effect_se - Numeric. Standard error of that effect.
\item z - Numeric. The Wald statistic.
\item p_values - Numeric. Two-sided p-values.
\item fdr - Numeric. Benjamini-Hochberg adjusted p-values.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
GPU equivalent of \code{bixverse::rs_nebula_sc}. Stage two of NEBULA, the
per-gene penalised fits, is dispatched to the WGPU backend in \code{f32} and
finished on the host in \code{f64}. Everything else, including the streaming of
the counts out of the gene-major store in batches, the subject ordering, the
dispersion shrinkage and the Wald test, is the CPU code. REML is not
implemented on the device and is rejected.
}
\references{
He, et al., Commun Biol, 2021
}
\keyword{internal}
