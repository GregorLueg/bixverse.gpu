% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_gpu.R
\name{find_neighbours_cagra_sc}
\alias{find_neighbours_cagra_sc}
\title{Find neighbours via CAGRA GPU-acceleration for single cells}
\usage{
find_neighbours_cagra_sc(
  object,
  embd_to_use = "pca",
  no_embd_to_use = NULL,
  modality = c("rna", "adt"),
  cagra_params = params_sc_cagra(),
  extract_knn = FALSE,
  snn_params = params_sc_neighbours(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{object}{\code{SingleCells} (or \code{SingleCellsMultiModal}) class.}

\item{embd_to_use}{String. The embedding to use.}

\item{no_embd_to_use}{Optional integer. Number of embedding dimensions to
use. If \code{NULL} all will be used.}

\item{modality}{String. One of \code{c("rna", "adt")}. You can only use \code{"adt"}
on \code{SingleCellsMultiModal} class.}

\item{cagra_params}{List. Output of \code{\link[=params_sc_cagra]{params_sc_cagra()}}.}

\item{extract_knn}{Logical. If \code{TRUE}, extracts the kNN graph directly from
the NNDescent result. If \code{FALSE}, runs beam search over the pruned CAGRA
graph. The extraction is faster, but creates a lower quality kNN graph.}

\item{snn_params}{List. Output of \code{\link[bixverse:params_sc_neighbours]{bixverse::params_sc_neighbours()}}. The
kNN graph-related parameters will be ignored in favour of \code{cagra_params}.}

\item{seed}{Integer. For reproducibility.}

\item{.verbose}{Boolean. Controls verbosity.}
}
\value{
The object with added kNN matrix and sNN graph in the selected
modality slot.
}
\description{
This function generates kNN data using the CAGRA (CUDA-Accelerated Graph
Retrieval Approximation) algorithm on the wgpu backend via the \code{bixverse.gpu}
package. CAGRA first builds a dense NNDescent graph, then prunes it into a
sparser navigational graph optimised for beam-search traversal. Two retrieval
modes are available: direct extraction from the NNDescent graph
(\code{extract_knn = TRUE}), which is faster but slightly less precise, or beam
search over the pruned CAGRA graph (\code{extract_knn = FALSE}), which is slower
but yields higher recall. Subsequently, the kNN data is used to generate an
sNN igraph for downstream clustering.
}
\note{
Euclidean distance calculates the squared Euclidean distance for speed.
}
