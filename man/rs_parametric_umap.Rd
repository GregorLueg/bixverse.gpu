% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_parametric_umap}
\alias{rs_parametric_umap}
\title{Parametric UMAP implementation}
\usage{
rs_parametric_umap(
  data,
  n_dim,
  k,
  min_dist,
  spread,
  parametric_params,
  seed,
  verbose,
  use_gpu
)
}
\arguments{
\item{data}{Numerical matrix. Data of dimensions samples x features.}

\item{n_dim}{Integer. Number of embedding dimensions.}

\item{k}{Integer. Number of nearest neighbours for graph construction.}

\item{min_dist}{Numeric. Minimum distance between embedded points.}

\item{spread}{Numeric. Effective scale of embedded points.}

\item{parametric_params}{Named list. Merged parametric UMAP parameters
containing nearest neighbour, graph, and training configuration.}

\item{seed}{Integer. Seed for reproducibility.}

\item{verbose}{Boolean. Controls verbosity.}

\item{use_gpu}{Logical. If \code{TRUE}, trains on the wgpu backend. If
\code{FALSE}, trains on the CPU via NdArray. Defaults to \code{TRUE}.}
}
\value{
A named list with two elements: \code{embedding} (numerical matrix of
dimensions samples x n_dim) and \code{model} (external pointer to the trained
encoder for use with \code{rs_parametric_umap_predict}).
}
\description{
Trains a neural network encoder to learn a mapping from the input space to a
low-dimensional embedding that preserves the UMAP graph structure. Supports
both GPU (wgpu) and CPU (NdArray) backends. For small to medium data sets
(fewer than ~10k samples or narrow hidden layers), the CPU backend is
typically faster owing to GPU kernel dispatch overhead.
}
