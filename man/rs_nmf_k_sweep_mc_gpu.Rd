% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_nmf_k_sweep_mc_gpu}
\alias{rs_nmf_k_sweep_mc_gpu}
\title{Sweep k on the GPU over meta cells}
\usage{
rs_nmf_k_sweep_mc_gpu(
  sparse_data,
  k_range,
  preprocessing,
  use_second_layer,
  nmf_hals_params,
  nmf_consensus_params,
  n_runs,
  seed,
  verbose
)
}
\arguments{
\item{sparse_data}{A named list with \code{data}, \code{indptr}, \code{indices}, \code{nrow},
\code{ncol} and \code{cs_type}.}

\item{k_range}{Integer vector. Ranks to evaluate, every entry at least 2 and
at most 128, the GPU solver's rank cap.}

\item{preprocessing}{String. One of \code{c("none", "sd", "sqrt_sd")}.}

\item{use_second_layer}{Boolean. If \code{TRUE}, runs NMF on normalised counts.}

\item{nmf_hals_params}{Named list. Contains the NMF parameters.}

\item{nmf_consensus_params}{Named list. Contains the consensus parameters.}

\item{n_runs}{Integer. Number of restarts per k. Must be at least 2.}

\item{seed}{Integer. Base random seed.}

\item{verbose}{Integer. \code{0L} - quiet; \code{1L} - normal verbosity; \code{2L} -
detailed verbosity.}
}
\value{
A list of equal-length vectors, one element per swept k: \code{k},
\code{stability}, \code{best_error}, \code{median_error}, \code{consensus_failed}, \code{n_dropped},
\code{n_empty_clusters} and \code{n_converged}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
GPU counterpart of \code{\link[bixverse:rs_nmf_k_sweep_mc]{bixverse::rs_nmf_k_sweep_mc()}}. Returns diagnostics
only, no factors. The matrix is uploaded once and serves every one of the
\code{length(k_range) * n_runs} solves, which is where the GPU path pays off.
}
\references{
Kotliar et al., eLife, 2019
}
\keyword{internal}
