# Check Harmony v2 GPU parameters

Checkmate extension for checking Harmony v2 GPU parameters.

## Usage

``` r
checkScHarmonyParamsV2Gpu(x)
```

## Arguments

- x:

  The list to check/assert.

## Value

`TRUE` if the check was successful, otherwise an error message.
