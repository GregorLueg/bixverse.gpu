# Load a parametric UMAP as a qs2 file

Load a parametric UMAP as a qs2 file

## Usage

``` r
load_parametric_umap(path)
```

## Arguments

- path:

  String. The path to the serialised trained parametric UMAP model on
  disk.

## Value

The `ParametricUmapModel`.
