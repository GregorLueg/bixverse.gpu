# Check t-SNE parameters (GPU version)

Checkmate extension for checking the t-SNE parameters (GPU version).

## Usage

``` r
checkTsneParamsGpu(x)
```

## Arguments

- x:

  The list to check.

## Value

`TRUE` if the check was successful, otherwise an error message.
