# Print a KMeansClusterGPU object

Print a KMeansClusterGPU object

## Usage

``` r
# S3 method for class 'KMeansClusterGPU'
print(x, ...)
```

## Arguments

- x:

  A `KMeansClusterGPU` object.

- ...:

  Further arguments (ignored).

## Value

Invisibly returns `x`.
