# Check UMAP parameters (GPU version)

Checkmate extension for checking the UMAP parameters (GPU version).

## Usage

``` r
checkUmapParamsGpu(x)
```

## Arguments

- x:

  The list to check.

## Value

`TRUE` if the check was successful, otherwise an error message.
