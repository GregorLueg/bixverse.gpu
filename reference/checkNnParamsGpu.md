# Check GPU nearest neighbour parameters

Checkmate extension for checking the GPU nearest neighbour parameters.

## Usage

``` r
checkNnParamsGpu(x)
```

## Arguments

- x:

  The list to check.

## Value

`TRUE` if the check was successful, otherwise an error message.
